/**
 * Login Page Component
 * 
 * This page renders the secure login form and handles authentication flow.
 */

import LoginForm from '../../../components/auth/LoginForm';

export default function LoginPage(): React.ReactNode {
  return <LoginForm />;
}